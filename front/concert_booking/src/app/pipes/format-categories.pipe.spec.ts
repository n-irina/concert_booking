import { FormatCategoriesPipe } from '../format-categories.pipe';

describe('FormatCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
