import { FormatArtistsPipe } from './format-artists.pipe';

describe('FormatArtistsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatArtistsPipe();
    expect(pipe).toBeTruthy();
  });
});
